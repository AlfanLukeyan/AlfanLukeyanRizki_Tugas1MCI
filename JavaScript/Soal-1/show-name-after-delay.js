const displayNameAfterDelay = function (name) {
    console.log(`Saya Calon Admin MCI`);
  
    /* The setTimeout() method calls a function after a number of milliseconds. */
    setTimeout(function () {
      console.log(name);
    }, 3000);
  };
  
  /* fill in the function parameter with your name */
  displayNameAfterDelay("Alfan Lukeyan Rizki");